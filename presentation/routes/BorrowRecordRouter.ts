import express from "express";

export const borrowRecordRouter = express.Router();

borrowRecordRouter.get("/", () => {
});
borrowRecordRouter.get("/:id", () => {
});
borrowRecordRouter.post("/", () => {
});
borrowRecordRouter.delete("/:id", () => {
});