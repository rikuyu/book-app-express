import express from "express";

export const bookRouter = express.Router();

bookRouter.get("/", () => {
});
bookRouter.get("/:id", () => {
});
bookRouter.post("/", () => {
});
bookRouter.delete("/:id", () => {
});